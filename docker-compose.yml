services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - PORT=3000
      - DATABASE_URL=pagesnap.db
      - PAYMENT_GATEWAY=stripe
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped
